#include <iostream>

long long silnia(int n)
{
    if (n <= 1) return 1;
    return n * silnia(n - 1);
}
int main()
{
    int n;
    std::cout << "Wpisz n: " << std::endl;
    std::cin >> n;
    std::cout << "Silnia " << n << " wynosi: " << silnia(n) << std::endl;
    return 0;
}
